# Normalize line endings: store LF in the repo, check out native. Silences the
# "LF will be replaced by CRLF" warnings and avoids spurious whole-file diffs.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
